# 개발 히스토리 및 작업 로그

**프로젝트**: 학교 행정매뉴얼 RAG 챗봇  
**개발 기간**: 2026년 1월 17일  
**개발자**: deoksangcho  
**GitHub**: https://github.com/deoksangcho/chat_manual

---

## 📋 프로젝트 개요

### 목표
416페이지 분량의 "2025 학교 업무매뉴얼 행정(최종).pdf"를 기반으로 학교 행정 업무 담당자를 위한 AI 챗봇 개발

### 핵심 요구사항
- 구조화된 답변 형식 (질문 요지 → 절차 → 법령 → 서식 → 주의사항 → 출처)
- 페이지 번호 명시
- 상세하고 실무에 활용 가능한 답변
- Streamlit Cloud 배포

---

## 🔧 개발 과정

### Phase 1: 환경 설정 (완료)

#### 1-1. 가상환경 및 의존성 설치
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

#### 1-2. 환경변수 설정
- `.env` 파일 생성
- `OPENAI_API_KEY` 설정
- `.gitignore`에 추가

#### 1-3. 주요 패키지
- OpenAI: `openai==1.12.0`
- LangChain: `langchain==0.1.6`, `langchain-openai==0.0.8`
- ChromaDB: `chromadb==0.4.22`
- BM25: `rank-bm25==0.2.2`
- Streamlit: `streamlit==1.31.0`

---

### Phase 2: PDF 분석 (완료)

#### 2-1. PDF 구조 분석
```bash
python scripts/analyze_pdf.py
```

**결과**:
- 총 페이지: 416 페이지
- 파일 크기: 9.34 MB
- 중분류 섹션: 494개
- 법령 조항: 371개
- 시행령: 244개

#### 2-2. 주요 발견사항
- 계층적 구조: 로마숫자 → 숫자 → 하이픈
- 메타데이터: 법령, 서식 정보 포함
- 텍스트 추출 가능

---

### Phase 3: 데이터베이스 생성 (완료)

#### 3-1. 초기 설정
```yaml
chunking:
  chunk_size: 1200
  chunk_overlap: 150
```

#### 3-2. 최종 설정 (성능 개선)
```yaml
chunking:
  chunk_size: 1500  # 1200 → 1500
  chunk_overlap: 200  # 150 → 200

retrieval:
  vector_top_k: 12  # 10 → 12
  bm25_top_k: 12    # 10 → 12
  final_top_k: 10   # 5 → 8 → 10

llm:
  model: "gpt-4o-mini"
  temperature: 0.0  # 0.1 → 0.0
  max_tokens: 3000  # 1500 → 2000 → 3000
```

#### 3-3. 데이터베이스 생성 결과
```bash
python scripts/create_database.py
```
- 총 청크 수: **468개** (605 → 468, chunk_size 증가로 감소)
- 소요 시간: 11-16초
- ChromaDB: `./data/chroma_db`
- BM25 인덱스: `./data/bm25_index.pkl`

---

### Phase 4: Streamlit 앱 개발 (완료)

#### 4-1. 주요 기능
1. **사이드바**
   - API 키 입력 (배포 시 Secrets 사용)
   - 시스템 정보 표시
   - 사용 가이드
   - 대화 내역 초기화

2. **채팅 인터페이스**
   - 메시지 히스토리
   - 스트리밍 응답
   - 에러 처리

3. **보안 강화**
   - 로컬: `.env` + password 타입 입력
   - 배포: Streamlit Secrets (사용자에게 숨김)

#### 4-2. API 키 보안 처리
```python
# 배포 환경: Secrets 자동 로드
try:
    if "OPENAI_API_KEY" in st.secrets:
        api_key = st.secrets["OPENAI_API_KEY"]
        st.success("✅ API 키가 안전하게 로드되었습니다")
except (FileNotFoundError, AttributeError):
    # 로컬 환경: .env 또는 사용자 입력
    api_key = st.text_input("OpenAI API 키", type="password")
```

---

## 🐛 주요 문제 해결

### 문제 1: OpenAI 버전 호환성

**증상**: 
```
AttributeError: 'openai' module has no attribute 'ChatCompletion'
```

**원인**: openai 2.x와 langchain 호환 문제

**해결**:
```bash
pip install "openai<2.0,>=1.10.0"
# openai==1.109.1로 다운그레이드
```

---

### 문제 2: ChromaDB 메타데이터 검증 오류

**증상**:
```
Expected str/int/float/bool, got None
```

**원인**: PDF에서 추출한 메타데이터에 None 값과 리스트 포함

**해결**: `pdf_processor.py`의 `chunk_documents()` 수정
```python
# None 값을 문자열로 변환
for key, value in metadata.items():
    if value is None:
        clean_metadata[key] = "없음"
    elif isinstance(value, list):
        clean_metadata[key] = ", ".join(str(v) for v in value)
    else:
        clean_metadata[key] = value
```

---

### 문제 3: 토큰 제한 초과

**증상**:
```
Requested 518279 tokens, max 300000
```

**원인**: 605개 청크를 한 번에 임베딩 시도

**해결**: 배치 처리 구현
```python
batch_size = 100
for i in range(0, len(chunks), batch_size):
    batch = chunks[i:i + batch_size]
    if vectorstore is None:
        vectorstore = Chroma.from_documents(batch, embeddings, persist_directory)
    else:
        vectorstore.add_documents(batch)
```

---

### 문제 4: 검색 결과 없음

**증상**: 하이브리드 검색이 결과를 반환하지 않음

**해결**: 
1. 일시적으로 벡터 검색만 사용
2. 나중에 하이브리드 검색 복원 및 개선

---

### 문제 5: 페이지 번호 누락

**증상**: 출처 섹션에 페이지 번호가 표시되지 않음

**해결**: SYSTEM_PROMPT 강화
```python
# 명시적인 예시 추가
**예시:**
- 급여 관리 규정 (페이지 45)
- 징계 처리 절차 (페이지 127)
```

---

### 문제 6: 답변이 너무 단순함

**증상**: 각 섹션이 1-2줄로 간략함 (378자)

**해결 1단계**: config.yaml 파라미터 증가
- `final_top_k`: 5 → 8
- `chunk_size`: 1200 → 1500
- `max_tokens`: 1500 → 2000

**해결 2단계**: SYSTEM_PROMPT 대폭 강화
- 각 섹션 최소 100자 이상 요구
- 절차: 담당자, 기한, 방법, 주의사항 모두 포함
- 법령: 조문 내용 + 실무 적용 방법
- 서식: 작성법, 제출처, 보관 기한
- 주의사항: 최소 5개, 실수 사례 포함
- 구체적인 예시 추가

**해결 3단계**: 하이브리드 검색 복원
- 벡터 검색 (의미 기반) + BM25 검색 (키워드 기반)
- 중복 제거 로직 추가
- final_top_k: 8 → 10

**해결 4단계**: 최종 조정
- `temperature`: 0.1 → 0.0 (일관성)
- `max_tokens`: 2000 → 3000 (더 긴 답변)

**결과**: 답변 길이 378자 → 800-2000자로 증가

---

### 문제 7: 배포 오류 (tiktoken Python 3.13 호환성)

**증상**:
```
error: can't find Rust compiler
× Failed to download and build `tiktoken==0.5.2`
```

**원인**: 
- `tiktoken 0.5.2`가 Python 3.13과 호환되지 않음
- Rust 컴파일러 필요
- `langchain-openai 0.0.2`가 구버전

**해결**:
```txt
# requirements.txt 수정
langchain-openai==0.0.8  # 0.0.2 → 0.0.8
tiktoken>=0.5.2          # 명시적 추가
numpy<2.0.0              # 버전 제약

# packages.txt 추가
build-essential
```

---

## 🎨 프롬프트 최적화 과정

### 초기 프롬프트
- 간단한 형식 지정
- 일반적인 지시사항

### 1차 개선
- 각 섹션별 상세 요구사항
- 페이지 번호 명시 강조

### 2차 개선 (최종)
- **100자 이상** 최소 길이 요구
- **구체적인 작성 예시** 추가
- **실무 활용 가능한 수준** 강조
- 각 섹션별 세부 요구사항:
  - 절차: 담당자, 기한, 방법, 주의사항
  - 법령: 조문 내용 + 실무 적용법
  - 서식: 작성법, 제출처, 보관 기한
  - 주의사항: 실수 사례, 예외 상황, 실무 팁

---

## 🔍 검색 시스템 진화

### 초기: 벡터 검색만
- ChromaDB similarity_search
- `top_k=5`

### 중기: 하이브리드 검색 (1차)
- Vector + BM25
- RRF(Reciprocal Rank Fusion) 시도
- 문제: 복잡도 증가, 안정성 저하

### 안정화: 벡터 검색만 (임시)
- 안정성 우선
- `top_k=8`로 증가

### 최종: 하이브리드 검색 (2차)
- Vector (의미 기반) + BM25 (키워드 기반)
- 단순 병합 + 중복 제거
- `vector_top_k=12`, `bm25_top_k=12`, `final_top_k=10`
- 벡터 검색 우선, BM25로 보완

---

## 📊 성능 지표

### 데이터베이스
- **청크 수**: 468개
- **임베딩 차원**: 1536 (text-embedding-3-small)
- **생성 시간**: 11-16초

### 응답 품질
- **검색 시간**: 1-2초
- **응답 생성**: 3-5초
- **평균 답변 길이**: 800-2000자 (이전: 378자)
- **컨텍스트**: 10개 문서 (이전: 5개)

### 답변 구조
- ✅ 질문 요지 정리: 2-3문장
- ✅ 절차: 3-5단계 상세 설명
- ✅ 관련 법령: 조문 + 실무 적용
- ✅ 서식: 번호, 작성법, 제출처, 보관
- ✅ 주의사항: 5개 이상, 실무 팁
- ✅ 출처: 페이지 번호 포함

---

## 🚀 배포 과정

### GitHub 설정
```bash
git init
git branch -M main
git remote add origin https://github.com/deoksangcho/chat_manual.git
git add .
git commit -m "Initial commit: 학교 행정매뉴얼 RAG 챗봇 v1.0"
git push -u origin main
```

### Streamlit Cloud 설정
1. https://share.streamlit.io/ 로그인
2. New app 생성
3. Repository: `deoksangcho/chat_manual`
4. Branch: `main`
5. Main file: `app.py`
6. Secrets 추가:
```toml
OPENAI_API_KEY = "sk-proj-..."
```

### 배포 오류 및 해결
- ❌ tiktoken 빌드 실패 → requirements.txt 버전 업데이트
- ✅ 재배포 성공

---

## 📁 최종 파일 구조

```
chat_manual/
├── app.py                      # Streamlit 메인 앱
├── config.yaml                 # 설정 파일
├── requirements.txt            # 패키지 의존성
├── packages.txt               # 시스템 패키지 (build-essential)
├── .env                       # 환경변수 (로컬)
├── .env.example              # 환경변수 예시
├── .gitignore                # Git 제외 파일
├── README.md                 # 프로젝트 문서
├── PLAN.md                   # 개발 계획
├── PROCEED.md                # 실행 가이드
├── DEVELOPMENT_HISTORY.md   # 개발 히스토리 (현재 파일)
├── 2025 학교 업무매뉴얼 행정(최종).pdf
├── .streamlit/
│   ├── config.toml          # Streamlit 설정
│   └── secrets.toml         # API 키 (로컬, Git 제외)
├── data/
│   ├── chroma_db/           # ChromaDB 벡터 저장소
│   └── bm25_index.pkl       # BM25 인덱스
├── scripts/
│   ├── analyze_pdf.py       # PDF 구조 분석
│   └── create_database.py   # DB 생성
└── src/
    ├── __init__.py
    ├── pdf_processor.py     # PDF 파싱 및 청킹
    ├── vectorstore.py       # 벡터 DB 관리
    ├── rag_chain.py         # RAG 파이프라인
    └── response_formatter.py # 응답 포맷팅
```

---

## 🔑 핵심 설정값 (최종)

### config.yaml
```yaml
pdf:
  source_file: "2025 학교 업무매뉴얼 행정(최종).pdf"

chunking:
  chunk_size: 1500
  chunk_overlap: 200
  separators: ["\n\n\n", "\n\n", "\n", ". ", " "]

embedding:
  model: "text-embedding-3-small"
  dimensions: 1536

llm:
  model: "gpt-4o-mini"
  temperature: 0.0
  max_tokens: 3000

retrieval:
  vector_top_k: 12
  bm25_top_k: 12
  final_top_k: 10

database:
  chroma_path: "./data/chroma_db"
  bm25_path: "./data/bm25_index.pkl"
```

### 환경변수
```bash
OPENAI_API_KEY=sk-proj-...
```

---

## 💡 교훈 및 베스트 프랙티스

### 1. 버전 호환성
- ✅ OpenAI 패키지는 1.x 버전 사용 (langchain 호환성)
- ✅ Python 버전별 패키지 호환성 확인 필수
- ✅ tiktoken, numpy 버전 제약 명시

### 2. 메타데이터 처리
- ✅ None 값, 리스트 타입을 ChromaDB 호환 형식으로 변환
- ✅ 메타데이터 클리닝은 청킹 단계에서 처리

### 3. 토큰 제한
- ✅ 대용량 데이터는 배치 처리 (100개씩)
- ✅ 임베딩 API 호출 최적화

### 4. 프롬프트 엔지니어링
- ✅ 구체적인 예시 제공이 가장 효과적
- ✅ 최소 길이 명시로 상세도 보장
- ✅ 각 섹션별 세부 요구사항 명확히

### 5. 검색 전략
- ✅ 하이브리드 검색 (Vector + BM25)
- ✅ 단순한 병합이 복잡한 RRF보다 안정적
- ✅ top_k 값은 실험을 통해 최적화

### 6. 보안
- ✅ API 키는 절대 Git에 커밋하지 않기
- ✅ `.env`, `secrets.toml` 모두 `.gitignore` 추가
- ✅ 배포 환경은 Secrets 사용

### 7. 디버깅
- ✅ 개발 시 DEBUG 로그 활용
- ✅ 배포 전 DEBUG 로그 제거
- ✅ 에러 처리 및 사용자 친화적 메시지

---

## 📈 향후 개선 방향

### 단기 (1-2주)
- [ ] 응답 속도 최적화 (캐싱)
- [ ] 검색 정확도 평가 및 개선
- [ ] 사용자 피드백 수집 기능

### 중기 (1-2개월)
- [ ] 다국어 지원 (영어)
- [ ] 문서 업데이트 자동화
- [ ] 대화 히스토리 기반 컨텍스트 유지

### 장기 (3개월+)
- [ ] Fine-tuning 고려
- [ ] 다른 학교 매뉴얼 지원
- [ ] 음성 입력/출력 지원

---

## 📞 문의 및 지원

- **GitHub**: https://github.com/deoksangcho/chat_manual
- **Issues**: 버그 리포트 및 기능 제안
- **Discussions**: 일반 질문 및 토론

---

**마지막 업데이트**: 2026년 1월 17일  
**버전**: v1.0  
**상태**: ✅ 배포 완료
